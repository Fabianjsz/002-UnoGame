Classes:

class Karte:  -> Check
    -> Node mit color, value und next
    -> !!!! ZUERST KOMMT COLOR DAN VALUE (fixed)

class Deck:
    -> Stack mit 108 Karten
    -> buildDeck():
        Builds a shuffled uno deck into an array (L:108)

fun BuildDeck:
    -> Builds the array deck into a linked stack

class SpielerHand():
    -> ziehen(X):
         Draw x ammount of cards into hand

    -> spielKarte(Karte):
        play Card(X) onto stack if isPlayable() == True
    -> isPlayable():
        returns boolean wether card in playable or not


class Stack:
Stack, auf welchen nur neue values geadded werden können
funtkonen:
    clearStack():
        Löscht den Stack

    getValue():
        return lenght

